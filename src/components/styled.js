import styled from 'styled-components';

export const LinkText = styled.span`
  display: block;
  color: darkblue;
  transition: 0.3s linear;
  font-weight: 500;
  color: #000;
  &: hover {
    transform: scale(1.1);
  }
`;
